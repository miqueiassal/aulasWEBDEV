from fastapi import FastAPI
from sqlmodel import Session, select, SQLModel, create_engine
from contextlib import asynccontextmanager
from database import Post, Usuario, Obra 

arquivo_sqlite = "Projeto.db"
url_sqlite = f"sqlite:///{arquivo_sqlite}"

engine = create_engine(url_sqlite)

def create_db_and_tables():
    SQLModel.metadata.create_all(engine)




@asynccontextmanager
async def initFunction(app: FastAPI):
    # Executado antes da inicialização da API
    create_db_and_tables()
    yield
    # Executado ao encerrar a API

app = FastAPI(lifespan=initFunction)

@app.post("/usuario")
async def criarusuario(usuario: Usuario):
    with Session(engine) as session:
        session.add(usuario)
        session.commit()
        session.refresh(usuario)
        return usuario
    
@app.post("/obras")
async def registrarObras(obra: Obra):
    with Session(engine) as session:
        session.add(obra)
        session.commit()
        session.refresh(obra)
        return obra

@app.post("/post")
async def matricular(usuario_id: int, obra_id: int):
    with Session(engine) as session:
        post = Post(
            usuario_id=usuario_id,
            obra_id=obra_id
        )

        session.add(post)
        session.commit()

        return post

@app.patch("/post")
def mudarComentarios(username:str, obraNome:str, tipo:str,comentarios:str):
    obra = session.exec(select(Obra).where(Obra.nome == obraNome,Obra.tipo==tipo)).first()
    usuario = session.exec(select(Usuario).where(Usuario.username == username)).first()

    if not obra or not usuario:
        raise HTTPException(404, "Usuário ou obra não encontrado")
    
    query = select(Post).where(Post.usuario_id == usuario.id,
                               Post.obra_id == obra.id)
    
    post = session.exec(query).first()
    
    if not post:
        raise HTTPException(404, "Post não encontrado")
    post.comentarios=comentarios
    session.add(post)
    session.commit()
    session.refresh()
    return post

@app.patch("/post")
def mudarReacao(username:str, obraNome:str, tipo:str,reacao:str):
    obra = session.exec(select(Obra).where(Obra.nome == obraNome,Obra.tipo==tipo)).first()
    usuario = session.exec(select(Usuario).where(Usuario.username == username)).first()

    if not obra or not usuario:
        raise HTTPException(404, "Usuário ou obra não encontrado")
    
    query = select(Post).where(Post.usuario_id == usuario.id,
                               Post.obra_id == obra.id)
    
    post = session.exec(query).first()
    
    if not post:
        raise HTTPException(404, "Post não encontrado")
    post.reacao=reacao
    session.add(post)
    session.commit()
    session.refresh()
    return post
